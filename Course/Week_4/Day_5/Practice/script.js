let count = 1

console.log(`you have ${count} messege${count ===1 ? "":"s"}.`);


count = 5
console.log(`you have ${count} messege${count ===1 ? "":"s"}.`);

let speed = 130
console.log(`your speed is ${speed}, ${speed >= 120 ? "Too fast" : "safe speed"}`);

let isLightOn = true
console.log(`${isLightOn===true?"the light is on" : "The light is off"}`);


let score = 77
console.log(`${score>=90? "A" : score<90 && score>=80? "B": "Needs Improvment"}`);
